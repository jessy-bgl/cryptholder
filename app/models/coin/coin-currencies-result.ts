import { Instance, types, SnapshotOut } from "mobx-state-tree";

export const CoinCurrenciesResult = types.model({
  btc: types.maybeNull(types.number),
  eth: types.maybeNull(types.number),
  ltc: types.maybeNull(types.number),
  bch: types.maybeNull(types.number),
  bnb: types.maybeNull(types.number),
  eos: types.maybeNull(types.number),
  xrp: types.maybeNull(types.number),
  xlm: types.maybeNull(types.number),
  link: types.maybeNull(types.number),
  dot: types.maybeNull(types.number),
  yfi: types.maybeNull(types.number),
  usd: types.maybeNull(types.number),
  aed: types.maybeNull(types.number),
  ars: types.maybeNull(types.number),
  aud: types.maybeNull(types.number),
  bdt: types.maybeNull(types.number),
  bhd: types.maybeNull(types.number),
  bmd: types.maybeNull(types.number),
  brl: types.maybeNull(types.number),
  cad: types.maybeNull(types.number),
  chf: types.maybeNull(types.number),
  clp: types.maybeNull(types.number),
  cny: types.maybeNull(types.number),
  czk: types.maybeNull(types.number),
  dkk: types.maybeNull(types.number),
  eur: types.maybeNull(types.number),
  gbp: types.maybeNull(types.number),
  hkd: types.maybeNull(types.number),
  huf: types.maybeNull(types.number),
  idr: types.maybeNull(types.number),
  ils: types.maybeNull(types.number),
  inr: types.maybeNull(types.number),
  jpy: types.maybeNull(types.number),
  krw: types.maybeNull(types.number),
  kwd: types.maybeNull(types.number),
  lkr: types.maybeNull(types.number),
  mmk: types.maybeNull(types.number),
  mxn: types.maybeNull(types.number),
  myr: types.maybeNull(types.number),
  ngn: types.maybeNull(types.number),
  nok: types.maybeNull(types.number),
  nzd: types.maybeNull(types.number),
  php: types.maybeNull(types.number),
  pkr: types.maybeNull(types.number),
  pln: types.maybeNull(types.number),
  rub: types.maybeNull(types.number),
  sar: types.maybeNull(types.number),
  sek: types.maybeNull(types.number),
  sgd: types.maybeNull(types.number),
  thb: types.maybeNull(types.number),
  try: types.maybeNull(types.number),
  twd: types.maybeNull(types.number),
  uah: types.maybeNull(types.number),
  vef: types.maybeNull(types.number),
  vnd: types.maybeNull(types.number),
  zar: types.maybeNull(types.number),
  xdr: types.maybeNull(types.number),
  xag: types.maybeNull(types.number),
  xau: types.maybeNull(types.number),
  bits: types.maybeNull(types.number),
  sats: types.maybeNull(types.number),
});

// according to the official mobx-state-tree doc, using the following
// interface is much more optimized for the typescript compiler
// https://mobx-state-tree.js.org/tips/typescript#using-a-mst-type-at-design-time
export interface ICoinCurrenciesResult
  extends Instance<typeof CoinCurrenciesResult> {}
export interface ICoinCurrenciesResultSnapshot
  extends SnapshotOut<typeof CoinCurrenciesResult> {}
